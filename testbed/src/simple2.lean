import data.real.basic
import data.nat.factorial.basic

theorem a_plus_b_b_plus_a
(a b : ℕ) : a + b = b + a :=
begin
  sorry
end

theorem mod_arith_1
(x : ℕ) : x % 2 = 0 → (x * x) % 2 = 0 :=
begin
  sorry
end